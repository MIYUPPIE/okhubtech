node_modules
.next
out
build
dist

.env
.env.local
.env*.local
.dev.vars
.dev.vars.*

# wrangler's local state — D1 emulation (SQLite files), logs, etc.
.wrangler
.open-next

# generated Prisma client (prisma/schema.prisma's `output`) — regenerated by
# `prisma generate`, which @prisma/client's postinstall hook also runs
generated/

*.tsbuildinfo
next-env.d.ts

# SQLite dev database and its WAL/journal files — never ship real order data
# in the repo.
prisma/*.db
prisma/*.db-journal
prisma/*.db-wal
prisma/*.db-shm

npm-debug.log*
.DS_Store
